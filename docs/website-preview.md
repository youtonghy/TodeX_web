# 本地官网预览

- 官网：`http://127.0.0.1:5174/`
- 原面板：`http://127.0.0.1:5174/app`
- 预览命令：`pnpm dev --port 5174 --strictPort`，仅监听本机。
- 官网介绍根据 TodeX 各项目 README 整理；视觉参考航空模板的云空、胶囊导航、大标题与暖橙色按钮。
- Desktop 最新公开版本为 `v2.0.0`；Backend 为 `v2.0.1`，可切换至 `v2.0.0`。平台、架构、文件大小与下载地址均来自官方 GitHub Releases。
- 移动 App 仅展示暂缓发布状态。
- `/app` 独立加载原有面板。同一 origin 的既有浏览器数据保持可用；不同端口仍是不同 origin。
- [官网图片、生成方式和完整提示词](website-assets.md)；本地资源为 `public/images/cloud-sky.jpg`。

## 验证

- `pnpm build`：通过；原有大型工作台代码块仍有 Vite 体积提示。
- `pnpm typecheck`：通过。
- 对本次修改文件执行 ESLint：通过。
- 官网、下载、导航、`/app` 深层 URL 和浏览器存储隔离的 15 项浏览器测试：全部通过，覆盖桌面、平板和手机。
- 静态服务器与浏览器平台的 5 项单元测试：全部通过。
- 手动检查了桌面和手机首屏、工作台示意、能力介绍、版本下拉和迁移后的真实面板。

## 完整回归中的现有问题

本次没有修改面板组件、会话逻辑或共享协议。完整回归还出现以下范围内的问题：

- `devicePairing.ts` 的控制字符正则触发 `no-control-regex`。
- 聊天权限、Pi 扩展和工作台标签相关单元测试存在缺失 mock/API 或断言与当前界面不一致的问题。
- 原有平板/手机面板测试仍期望旧的“打开工作台”按钮及导航弹窗行为。

这些问题未在官网工作中更改。静态服务器测试首次因沙箱禁止监听端口而失败，获得本机监听权限后已通过。
